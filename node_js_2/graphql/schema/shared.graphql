# Solo definimos Image una vez aquí
type Image {
  src: String!
  alt: String!
}

# Tipos auxiliares que usaremos en las relaciones
type Tag {
  id: ID!
  description: String!
}

# Iniciamos los bloques Query y Mutation base
type Query {
  _empty: String
}

type Mutation {
  _empty: String
}